declare module '@material-ui/core/styles/zIndex' {
  export interface ZIndex {
    background: number
    max: number
  }
}

const zIndex = {
  background: -100,
  max: 5000,
}

export default zIndex
